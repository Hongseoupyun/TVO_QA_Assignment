// Verify the navigation bar
// Test cases:
// a. Verify the navigation bar is present
// b. Verify the navigation items
// c. Verify the navigation links
// d. Verify the navigation dropdown (if applicable)

describe("Verify Navigation Bar", () => {
  it("the navigation bar is present and visible on the page", () => {});
  it("the navigation bar contains multiple links", () => {});
  it("it navigates to homepage when clicking TVO logo", () => {});
  it("contents in navbar highlighted when hovering ", () => {});
});
